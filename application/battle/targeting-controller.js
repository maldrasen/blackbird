global.TargetingController = (function() {

  function startCommandTargeting(command) {
    console.log(`TODO: Start Targeting ${command}`);
  }

  return Object.freeze({
    startCommandTargeting
  });

})()