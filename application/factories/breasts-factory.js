global.BreastsFactory = (function() {

  // Building this next...
  function build(actor) {
    const breastData = { breastCount:2 };

    return breastData;
  }

  return Object.freeze({ build });

})();