global.FeatureFactory = (function() {

  // This will build and return a feature model. Before we can do that though, we need to have the floor data. We could
  // pull the floor data from the dungeon itself, but probably better to send in. Less coupled that way.
  function build(options={}) {

  }

  return Object.freeze({
    build
  })

})();
