global.GraphHelper = (function() {

  // The graph argument should be an "Adjacency List", which is a map of arrays where the arrays contain the graph keys
  // they are connected to.
  //   { a:[b,c], b:[a], c:[a] }
  function buildMinimumSpanningTree(graph) {

  }

  return Object.freeze({
    buildMinimumSpanningTree
  });

})();