global.OutfitFactory = (function() {

  // TODO: Build an outfit appropriate for the character. (Hmm... first I need an entire framework for defining what
  //       types of outfits there are. A character's gender, species and personality should influence their clothing.
  //       Before starting this, I need to think about what the clothing options even really look like.
  function buildAndEquip(characterId) {}

  return Object.freeze({ buildAndEquip });

})();
