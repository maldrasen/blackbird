Replace Array remove helper with filters.

The dungeon view needs to hide the main info and show a party panel.

Add a close handler for the game state frame when the game mode is changed. Modes that should show the state frame should open it.